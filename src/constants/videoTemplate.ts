export enum ShapeType {
  CIRCLE = 'CIRCLE',
  RECTANGLE = 'RECTANGLE',
}

export enum ClipType {
  AUDIO_CLIP = 'AUDIO_CLIP',
  VIDEO_CLIP = 'VIDEO_CLIP',
  TEXT_CLIP = 'TEXT_CLIP',
  SHAPE_CLIP = 'SHAPE_CLIP',
  IMAGE_CLIP = 'IMAGE_CLIP',
}

export enum DynamicPlaceholderType {
  TEXT = 'TEXT',
  SINGLE_SELECT = 'SINGLE_SELECT',
}

export enum CropType {
  FIT = 'FIT',
  FILL = 'FILL',
}

export enum TransitionPhase {
  ENTRY = 'ENTRY',
  EXIT = 'EXIT',
}

export enum TransitionSpeed {
  SLOW = 'SLOW',
  NORMAL = 'NORMAL',
  FAST = 'FAST',
}
